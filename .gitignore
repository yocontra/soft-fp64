build/
build-*/
cmake-build-*/
docs/
.cache/
.DS_Store
.vscode/
.idea/
*.o
*.a
*.dylib
*.so
*.bc
*.ll
compile_commands.json

# Bench runner JSON outputs (bench_soft_fp64 --json, compare.py inputs).
# The committed baseline lives in bench/baseline.json; everything else
# is a local run artifact.
/current*.json
/bench-*.json

# Vendored comparison libraries (cloned by bench/fetch_external.sh).
bench/external/

# Claude worktrees / local metadata
.claude/
.worktrees/
CLAUDE.md
